# open-cae-core

Shared runtime used by all FEP MCP servers.

It provides configuration loading, workspace path guards, executable whitelisting, safe subprocess execution, structured tool responses, job records, JSON manifests, artifact hashing, and evidence-chain writers.

The package contains no solver-specific arbitrary execution endpoint.
